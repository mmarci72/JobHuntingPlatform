import { ProjectPositionId } from './project-position-id.model';

describe('ProjectPositionId', () => {
  it('should create an instance', () => {
    expect(new ProjectPositionId()).toBeTruthy();
  });
});
